# No `secrets:` key. The called workflow still inherits `permissions:` — which
# is how npm trusted publishing mints its OIDC token — so "no secrets" is not
# "no credentials".
name: Reusable No Secrets
on:
  push:
    tags: ['v*']
jobs:
  publish:
    uses: ./.github/workflows/called-cache.yml
